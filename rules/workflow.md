# Workflow Rules

A task is NOT complete until all code changes are committed and pushed to the remote branch. Workflow: `git add`, `git commit` (conventional format), `git push`, then `git status` must be clean.
